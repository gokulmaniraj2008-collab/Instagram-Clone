import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'com.gokul.instaclone', appName: 'InstaClone', webDir: 'dist', bundledWebRuntime: false };
export default config;
